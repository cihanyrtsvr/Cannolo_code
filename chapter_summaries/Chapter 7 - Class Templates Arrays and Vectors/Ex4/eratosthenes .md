# Erohtnees problem
To solve this issue we need to check the values until the number we are searching.
Lets say that last numebr should be N so first
* until the sqrt(N) for loop
    - save the numbers that are prime numbers

* When loop finsihes the remaining numbers until N are all prime numbers
